name: Basic Setup Test

on:
  workflow_dispatch:  # This means we can trigger it manually first

jobs:
  test-setup:
    runs-on: ubuntu-latest
    
    steps:
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18' 